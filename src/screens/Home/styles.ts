import styled from 'styled-components';

export const Container = styled.div`
  height: 100vh;
  text-align: center;
  align-items: center;
  justify-content: center;

  h2 {
    position: relative;
    top: 50%;
    color: var(--shape);
  }
`;
